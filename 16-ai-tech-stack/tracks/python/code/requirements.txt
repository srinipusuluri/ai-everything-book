# Minimum to run the three scripts in this folder.
#   uv pip install -r requirements.txt
#
# vectorization_benchmark.py   -> numpy
# async_llm_client.py          -> stdlib only
# pydantic_structured_output.py-> pydantic (degrades to a stdlib fallback without it)
numpy>=2.0
pydantic>=2.9

# Everything below is discussed in the notes but not needed to run the scripts.
# Uncomment what you want to play with.
# httpx>=0.27               # async HTTP client for real provider calls
# pydantic-settings>=2.4    # typed config from env / .env
# polars>=1.0               # the fast dataframe
# pandas>=2.2               # the compatible dataframe
# pytest>=8.0               # tests
# pytest-asyncio>=0.24      # async tests
# pytest-cov>=5.0           # coverage
# ruff>=0.6                 # lint + format
# mypy>=1.11                # type checker
# py-spy>=0.3               # sampling profiler, works on a running process
